{
  "name": "marionettejs-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-only Marionette v5 website prototype",
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "test": "node --test test/*.test.mjs",
    "check": "npm run build && npm test"
  }
}
